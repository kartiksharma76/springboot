spring.application.name=demo
dog.name=sheru
dog.age=6
# ===============================
# = DATA SOURCE CONFIGURATION
# ===============================
#spring.datasource.url=jdbc:mysql://localhost:3306/Hostel?useSSL=false&serverTimezone=UTC
spring.datasource.url=jdbc:mysql://localhost:3306/hostel?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Kartik@2005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# = JPA / HIBERNATE CONFIGURATION
# ===============================
# Show SQL queries in the console
spring.jpa.show-sql=true

# Automatically create or update tables
spring.jpa.hibernate.ddl-auto=update

# Hibernate dialect for MySQL 8
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect